services:
  db:
    privileged: true
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-unicom_smoke}
      POSTGRES_USER: ${POSTGRES_USER:-unicom}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-unicom}
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U $${POSTGRES_USER} -d $${POSTGRES_DB}"]
      interval: 5s
      timeout: 5s
      retries: 12

  debug:
    privileged: true
    build:
      context: ../..
      dockerfile: smoke/consumer-install/Dockerfile
      target: base
    working_dir: /app
    volumes:
      - ./app:/app
      - ../../dist:/wheels:ro
    environment:
      DJANGO_SETTINGS_MODULE: smokeproject.settings
      DJANGO_PUBLIC_ORIGIN: http://localhost:18080
      POSTGRES_DB: ${POSTGRES_DB:-unicom_smoke}
      POSTGRES_USER: ${POSTGRES_USER:-unicom}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-unicom}
      POSTGRES_HOST: db
      POSTGRES_PORT: "5432"
      PYTHONUNBUFFERED: "1"
    depends_on:
      db:
        condition: service_healthy
    command: ["sleep", "infinity"]

  app:
    privileged: true
    build:
      context: ../..
      dockerfile: smoke/consumer-install/Dockerfile
      target: runtime
      args:
        UNICOM_INSTALL_SOURCE: ${UNICOM_INSTALL_SOURCE:-local-wheel}
        UNICOM_INSTALL_VERSION: ${UNICOM_INSTALL_VERSION:-}
    environment:
      DJANGO_SETTINGS_MODULE: smokeproject.settings
      DJANGO_PUBLIC_ORIGIN: http://localhost:18080
      POSTGRES_DB: ${POSTGRES_DB:-unicom_smoke}
      POSTGRES_USER: ${POSTGRES_USER:-unicom}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-unicom}
      POSTGRES_HOST: db
      POSTGRES_PORT: "5432"
      PYTHONUNBUFFERED: "1"
    depends_on:
      db:
        condition: service_healthy
    command: >
      sh -c "
      python manage.py migrate &&
      python seed_webchat_channel.py &&
      python manage.py runserver 0.0.0.0:8000
      "
    ports:
      - "18080:8000"
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://127.0.0.1:8000/unicom/webchat/demo/').read()"]
      interval: 5s
      timeout: 5s
      retries: 12

  smoke:
    privileged: true
    build:
      context: ../..
      dockerfile: smoke/consumer-install/Dockerfile
      target: runtime
      args:
        UNICOM_INSTALL_SOURCE: ${UNICOM_INSTALL_SOURCE:-local-wheel}
        UNICOM_INSTALL_VERSION: ${UNICOM_INSTALL_VERSION:-}
    environment:
      SMOKE_BASE_URL: http://app:8000
      POSTGRES_DB: ${POSTGRES_DB:-unicom_smoke}
      POSTGRES_USER: ${POSTGRES_USER:-unicom}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-unicom}
      POSTGRES_HOST: db
      POSTGRES_PORT: "5432"
    depends_on:
      db:
        condition: service_healthy
      app:
        condition: service_healthy
    command: ["python", "smoke_test.py"]
